{
  "name": "portfulo-worker-push",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker — bridges Supabase row events to APNS push notifications for Portfulo iOS",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.79.0"
  },
  "dependencies": {
    "@sentry/cloudflare": "^10.53.1"
  }
}
